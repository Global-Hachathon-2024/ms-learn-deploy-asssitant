from typing import Tuple

def validate_bicep(bicep: str) -> Tuple[bool, str]:
    """
    Validate the given Bicep file
    Args:
        bicep (str): Bicep file content
    Returns:
        bool: True if the Bicep file is valid, False otherwise
        str: Error message if the Bicep file is invalid
    """
    return True, "error message"